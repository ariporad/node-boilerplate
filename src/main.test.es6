import main from 'main';

describe('The Server', () => {
  it('Should export an object', () => {
    // FIXME: All ES6 exports are objects, should check for exports.default
    main.should.be.an('object');
  });
  it('Should create an Express server.', () => {
    it('Should import Express', () => {
    });
    // TODO: Add Real Tests
  });
});
